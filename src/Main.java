public class Main {
    public static void main(String[] args) {
        Disk disk = new Disk();
        //disk.setid(0);
        System.out.println(disk.getid());
        Disk disk2 = new Disk();
        System.out.println(disk2.getid());
    }
}